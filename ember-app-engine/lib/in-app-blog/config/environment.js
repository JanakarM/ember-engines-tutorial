/* eslint-env node */
'use strict';

module.exports = function(environment) {
  let ENV = {
    modulePrefix: 'in-app-blog',
    podModulePrefix: 'in-app-blog/pods',
    environment
  };

  return ENV;
};
